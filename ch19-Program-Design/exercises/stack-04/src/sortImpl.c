#include <stdio.h>
#include <stdlib.h>

#include "ccStack.h"

/*
int sort_stack(int n)
{
  static int i = 0;

  CStack cs;

  printf("%d -> hello\n", cs->top);

  if(n <= 1)
    return 1;

  i = i + 1;

  return n * sort_stack(n - 1);
}
*/
